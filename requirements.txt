# Lead Prospector - Dependencies
# Python 3.11+ (Python 3.13 compatible)

# HTTP & API clients
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0

# HTML parsing & web scraping
beautifulsoup4>=4.12.0
lxml>=5.1.0
html5lib>=1.1

# Browser automation (undetected Chrome - anti-bot)
undetected-chromedriver>=3.5.0
selenium>=4.20.0
webdriver-manager>=4.0.0
setuptools>=69.0.0  # Required for Python 3.12+ (provides distutils)

# Data handling (use latest compatible versions)
pandas>=2.2.0
openpyxl>=3.1.0  # Excel export

# Environment & configuration
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Async utilities
asyncio-throttle>=1.0.0
tenacity>=8.2.0  # Retry logic

# Logging & progress
rich>=13.7.0  # Beautiful CLI output
tqdm>=4.66.0  # Progress bars

# URL & text processing
tldextract>=5.1.0
validators>=0.22.0

# Rate limiting
ratelimit>=2.2.0

# Web app backend (FastAPI)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
